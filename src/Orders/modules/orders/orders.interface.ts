
// Define the TOrder interface
export interface TOrder {
  email: string;
  productId: string;
  price: number;
  quantity: number;
}

// Define the TUser interface
export interface TUser {
  name: string;
  address: string;
  number: string;
  email: string;
}
